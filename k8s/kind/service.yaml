apiVersion: v1
kind: Service
metadata:
  name: agentic-ui
  namespace: agentic-ui
spec:
  type: NodePort
  selector:
    app: agentic-ui
  ports:
    - port: 80
      targetPort: 4000
      nodePort: 30080
